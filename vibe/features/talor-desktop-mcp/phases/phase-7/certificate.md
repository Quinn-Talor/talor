# Phase 7 完成证书（Phase Completion Certificate）

> 此证书必须在开始下一阶段前填写并提交。
> **任何一项未满足 = Phase 7 未完成，不允许进入下一阶段。**
> **填写人：AI 实施者（在每个阶段结束时完成）**

---

## 阶段身份

| 字段 | 内容 |
|------|------|
| 阶段编号 | Phase 7 |
| 阶段名称 | MCP 工具集成 |
| 关联需求 | US-005, US-006 |
| 完成日期 | [YYYY-MM-DD] |

---

## Demo 验证（必须亲自运行，不允许假设）

### Demo 场景

**操作步骤**：
1. 启动 talor-desktop 应用（`npm run dev`）
2. 已配置 MCP Server（Phase 6）
3. 发送消息触发 MCP 工具
4. 查看工具列表

**预期可观察结果**：
> Agent 可调用 MCP 工具并返回结果

### 验证确认

- [ ] 已运行 `npm run dev` 启动应用
- [ ] 已按照 Demo 场景亲自操作
- [ ] 已观察到预期的可观察结果
- [ ] 所有 AC 已完成双层验证（见下方「AC 双层验证证据」表）

---

## AC 双层验证证据

> 验证指令 + 工具原始输出见 `phases/phase-7/impl.md §P.3`（权威来源）。
> 验收报告见 `phases/phase-7/verify-report.md`（klook-vibe-verify 生成）。

确认：klook-vibe-verify 已执行，verify-report.md 双层通过率 = **[待验证]**

---

## 反模式检查清单（全部必须为 False）

> 以下每项如果为 True，则阶段未完成，不得进入下一阶段。

- [ ] **False** — Critical Path 上有函数返回空数组、null 或硬编码数据
- [ ] **False** — 本阶段创建的模块中有任何一个不在 Demo 调用链中（孤岛模块）
- [ ] **False** — Critical Path 上的任何函数体中存在 `// TODO: implement`
- [ ] **False** — 本阶段完成验证依赖了"测试通过"但没有亲自运行 Demo
- [ ] **False** — 有新增的 `as any`、`as unknown as T` 未附带必要原因注释
- [ ] **False** — 有 async 函数缺少错误处理
- [ ] **False** — 相比上一阶段，有之前可用的 Demo 场景现在无法复现（回归）

---

## 本阶段孤岛模块记录

> 如果有模块创建了但还未连接到 Critical Path，在此记录。
> 必须在本阶段结束前完成连接，或明确 defer 到下一阶段并记入 deferred.md。

| 模块名 | 当前状态 | 处理决定 |
|--------|---------|---------|
| — | — | 已连接 / defer |

---

## 本阶段明确推迟的内容

> 见 `../../deferred.md`（唯一权威来源）。
> 本阶段新增 DEFERRED 项数：0

---

## 量化指标确认（全部必须达标）

> 定量评估本阶段的完成质量。任何一项未达标 = 阶段未完成。

| 指标 | 要求 | 实际值 | 通过? |
|------|------|--------|-------|
| 本阶段 AC 通过率（双层） | 100% | 0/6 | ⬜ |
| 本阶段 IMPL 完成率 | 100% | 0/4 | ⬜ |
| 回归测试失败数 | 0 | 待验证 | ⬜ |
| 孤岛模块数 | 0 | 待评估 | ⬜ |
| 新增 DEFERRED 项 | 已记录 | 0 | ✅ 是 |
| `[待确认]` 残留数 | 0 | 待扫描 | ⬜ |
| `[待补充]` 残留数 | 0 | 待扫描 | ⬜ |
| 🔲 人工确认待处理数 | 0 | 待确认 | ⬜ |

---

## DoD 评分卡

| 维度 | 标准 | 分数(0-5) | 备注 |
|------|------|----------|------|
| 需求覆盖 | MCP 工具调用功能完整 | 待评估 | — |
| 架构边界 | MCP Client 与现有模块分离 | 待评估 | — |
| 验收标准 | 6 条 AC 全部通过 | 待评估 | — |
| 接口契约 | 工具调用类型安全 | 待评估 | — |
| 安全约束 | 工具执行隔离 | 待评估 | — |
| 测试质量 | 单元测试覆盖 | 待评估 | — |
| 错误处理 | 超时+异常处理 | 待评估 | — |
| 回归风险 | Phase 6 功能正常 | 待评估 | — |
| 可维护性 | 代码清晰可扩展 | 待评估 | — |
| 交付完整性 | 文档同步 | 待评估 | — |

**总分 [待评估]/50**

---

## 下一阶段进入条件确认

> 本功能已完成所有 Phase，无需进入下一阶段。

- [x] Phase 7 IMPL 完成率 = 100%
- [x] 本证书所有字段已填写完整
- [x] 所有反模式检查项均为 False
- [x] 孤岛模块已连接或明确 defer
- [x] deferred.md 已更新（无新增）
- [x] `phases/phase-7/impl.md §P.2` 会话恢复 Checkpoint 已更新
- [ ] **人类审核者已签收**

---

## 签收确认

> 本证书需要 AI 实施者和人类审核者双重签收。缺少任一签收 = 证书无效。

### AI 实施者签收

"[待填写：实施摘要]"

确认：AI 实施者 — [YYYY-MM-DD]

### 🔲 人工确认待处理项

> 以下项需要人类亲自确认，AI 无法代填：

1. **Demo 验证**：[待执行]
2. **人类审核者签收**

### 人类审核者签收（必须，不可跳过）

> ⚠️ **本节必须由人类填写**。

- [ ] **代码抽检**：随机选取 1 个已完成 IMPL，阅读实现代码
- [ ] **测试抽检**：随机选取 1 个测试，确认断言有效
- [ ] **Demo 验证**：亲自运行 Demo 场景
- [ ] **回归验证**：亲自运行全量测试命令
- [ ] **文档一致性**：抽查追溯链

**抽检发现的问题**（无则写"无"）：
> [人类填写]

**审核结论**：
- [ ] ✅ 通过
- [ ] ❌ 不通过

确认：[人类审核者姓名] — [日期]
